
<div class="row panel">
  <div class="columns small-4">
    <%= link_to event do %>
    <img src="<%= event.pic_url %>"/>
    <%end%>
  </div>
  <div class="columns small-8">
    <h4><%= link_to event.title, event%></h4>
      <span class="price"><%=t(:price)%>:&nbsp;<i class="fa fa-cny"></i><%=event.price %></span><br>
      <span><%=t(:by)%>:&nbsp;<%=link_to event.user.username.html_safe,profile_path(event.user) %><span>   
  </div>
    <div class="columns small-6">
      <%= "[No. #{event.id.to_s.rjust(10,'0')}]" %>
     
    </div>
    <div class="columns small-3">
      <span class="participants">
       <i class="fa fa-users"></i>(<%=event.participants_count%>)
      </span>
    </div>
    <div class="columns small-3">
      <span class="comments">
        <i class="fa fa-comments"></i>(<%=event.comments_count%>)
      </span>
    </div>
</div>


