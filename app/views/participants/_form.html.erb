<div>
	<%if current_user.user_addresses.size>0%>
	<% if current_user.user_addresses.size == 1 %>
	<% address = current_user.user_addresses.first %>
	<% elsif current_user.user_addresses.where(default: 1).present?  %>
	<% address = current_user.user_addresses.where(default: 1).first %>
	<% else %>
	<% address = current_user.user_addresses.last %>
	<% end %>
	<%= link_to user_addresses_path(groupbuy_id: params[:groupbuy_id]) do  %>
	<div class="address-block">
		<div class="address-title left"><%= t(:default_address) %></div>
		<div class="default-address left">
			<span class="name"><%= address.name %></span>
			<span class="mobile"><%= address.mobile %></span>
			<div class="address"><%= address.address %></div>
		</div>
		<div class="right-arrow right"><i class="fa fa-2x fa-angle-right"></i></div>
		<div class="clear"></div>
	</div>
	<% end %>
	<%else%>
	<% @user_address = UserAddress.new%>
	<%=render 'user_addresses/form' ,:locals=>{@user_address=>@user_address} %>
	<%end%>
	<hr />

</div>
<%= form_for form_params(@parent, @participant), data: {abide: ''} do |f| %>
<%= render 'layouts/form_errors', object: @participant %>

<div>
	<div class="left goods-weight"><%= f.number_field :goods_amount, required: true,placeholder: t(:quantity)%></div>
	<div class=""><%=@parent.goods_unit%></div>
	<div class="left deliver-time">
		<fieldset>
			<legend><%= t(:deliverytime) %></legend>
			<span class="date left">
				<fieldset>

					<label class="">
						<input type="radio" name="date" value="<%= t(:weekday) %>">
						<%= t(:weekday) %>
					</label>
					<label class="">
						<input type="radio" name="date" value="<%= t(:weekend) %>">
						<%= t(:weekend) %>
					</label>
					<label class="">
						<input type="radio" name="date" value="<%= t(:anyday) %>" checked="checked">
						<%= t(:anyday) %>
					</label>
					<div class="clear"></div>
				</fieldset>
			</span>

			<span class="time left">
				<fieldset>
					<label class="">
						<input type="radio" name="time" value="<%= t(:morning) %>">
						<%= t(:morning) %>
					</label>
					<label class="">
						<input type="radio" name="time" value="<%= t(:afternoon) %>">
						<%= t(:afternoon) %>
					</label>
					<label class="">
						<input type="radio" name="time" value="<%= t(:anytime) %>" checked="checked">
						<%= t(:anytime) %>
					</label>
					<div class="clear"></div>
				</fieldset>
			</span>
			<div class="clear"></div>
		</fieldset>
	</div>
	<%#= show_error_message('团购数量，不能为空！') %>
</div>
  	<!-- <div>
	    <%#= f.text_area :name, required: true, placeholder:t(:name)%>
	    <%#= show_error_message('联系人/收货人，不能为空！') %>
   </div>
   <div>
	    <%#= f.text_area :mobile, required: true,placeholder:t(:mobile)%>
	    <%#= show_error_message('联系电话，不能为空！') %>
   	</div>
	<div>
	    <%#= f.text_area :address, required: true, placeholder:t(:address)%>
	    <%#= show_error_message('收货地址，不能为空！') %>
	  </div> -->
	  <div>
	  	<%= f.text_area :remark, class:'comment-area',placeholder: t(:remark)%>
	  	<%#= show_error_message('备注，请不要超过500字') %>
	  </div>
	  <%= f.submit '提交', class:'tiny button radius' %>
	  <% end %>
